---
phase: 08-localization-foundation-and-polish-default
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/i18n.py
  - app/locales/pl.json
  - app/locales/en.json
  - main.py
  - app/views/calendar_routes.py
autonomous: true
requirements:
  - I18N-01
  - I18N-04
must_haves:
  truths:
    - "Application has a single locale resolver that defaults to Polish for first-time users"
    - "Templates can read localized labels via translation keys rather than hardcoded English strings"
    - "Bilingual dictionaries (Polish + English) exist for all phase-8 surfaces"
  artifacts:
    - path: "app/i18n.py"
      provides: "Locale resolution and translation lookup contracts"
      exports:
        - "resolve_locale"
        - "translate"
        - "inject_template_i18n"
    - path: "app/locales/pl.json"
      provides: "Polish translation key map for app UI/API copy"
    - path: "app/locales/en.json"
      provides: "English translation key map used as fallback and phase-9 base"
    - path: "main.py"
      provides: "Application wiring that injects locale/i18n helpers into template responses"
  key_links:
    - from: "main.py"
      to: "app/i18n.py"
      via: "Jinja template helper registration"
      pattern: "templates|jinja_env|globals|resolve_locale|translate"
    - from: "app/views/calendar_routes.py"
      to: "app/i18n.py"
      via: "Locale passed to month/day template contexts"
      pattern: "TemplateResponse|locale"
---

<objective>
Define localization contracts and Polish-default runtime wiring used by all downstream Phase 8 implementation tasks.

Purpose: establish one source of truth for locale resolution and translation lookup before broad copy migration.
Output: i18n helper module, Polish/English locale dictionaries, and template wiring.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-localization-foundation-and-polish-default/08-CONTEXT.md
@.planning/phases/08-localization-foundation-and-polish-default/08-RESEARCH.md
@main.py
@app/views/calendar_routes.py
@app/templates/base.html

<interfaces>
From main.py:
```python
templates = Jinja2Templates(directory="app/templates")

@app.get("/", response_class=HTMLResponse)
async def root(request: Request, user=Depends(get_current_user)):
    return templates.TemplateResponse("calendar.html", {"request": request, "user": user, "now": datetime.utcnow()})
```

From app/views/calendar_routes.py:
```python
return templates.TemplateResponse("partials/month_grid.html", {..., "display_month_name": calendar.month_name[month], ...})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create localization contracts and bilingual resource files</name>
  <files>app/i18n.py, app/locales/pl.json, app/locales/en.json</files>
  <behavior>
    - Translation keys are loaded from JSON dictionaries with safe fallback behavior.
    - Locale resolver returns `pl` when no explicit user preference is set.
    - Translation helper supports interpolation placeholders needed by current UI/API messages.
  </behavior>
  <action>Create `app/i18n.py` exposing locale resolution (`pl` default), dictionary loading/caching, and translation helper functions. Add `app/locales/pl.json` and `app/locales/en.json` with keys for auth, calendar shell, modals, sync, invite, and common API messages used in this phase.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_calendar_views.py -k "calendar or invite"</automated>
  </verify>
  <done>Localization contracts and base dictionaries exist and can serve Polish defaults with English fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire i18n context into template rendering with Polish default</name>
  <files>main.py, app/views/calendar_routes.py, tests/test_calendar_views.py</files>
  <behavior>
    - Root, invite, month-grid, and day-events template responses include locale and translation helpers.
    - HTML root language attribute can be driven from resolved locale.
    - Existing page routes continue rendering without auth/session behavior regressions.
  </behavior>
  <action>Register i18n context injection in app startup/render flow and pass locale metadata through calendar view routes. Keep behavior backward-compatible with existing route signatures while making `locale` and translation function available to templates and partials.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_calendar_views.py tests/test_auth.py -k "calendar or invite or login"</automated>
  </verify>
  <done>Template render pipeline supports localization and defaults to Polish for users without explicit preference.</done>
</task>

</tasks>

<verification>
Run `.venv\Scripts\python.exe -m pytest -q tests/test_calendar_views.py tests/test_auth.py`.
</verification>

<success_criteria>
- A single i18n contract exists and is wired into template rendering.
- Polish is the default resolved locale.
- Translation dictionaries for Polish and English are available for phase-8 surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/08-localization-foundation-and-polish-default/08-01-SUMMARY.md`
</output>
