---
phase: 08-localization-foundation-and-polish-default
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - app/views/calendar_routes.py
  - app/templates/calendar.html
  - app/templates/partials/month_grid.html
  - app/templates/partials/day_events.html
  - tests/test_calendar_views.py
  - tests/test_events_api.py
autonomous: true
requirements:
  - I18N-01
  - I18N-05
must_haves:
  truths:
    - "Default locale renders Polish date/time conventions in month/day/quick-add visible outputs"
    - "Calendar rendering avoids hardcoded English month names and non-Polish fallback locales"
    - "Regression tests prove Polish-default formatting behavior"
  artifacts:
    - path: "app/views/calendar_routes.py"
      provides: "Locale-aware month/day labeling inputs passed to templates"
    - path: "app/templates/calendar.html"
      provides: "Locale-aware JS date/time format helpers with pl default"
    - path: "tests/test_calendar_views.py"
      provides: "Coverage for default Polish labels and localized rendering markers"
  key_links:
    - from: "app/views/calendar_routes.py"
      to: "app/templates/partials/month_grid.html"
      via: "Localized month/day labels in template context"
      pattern: "display_month_name|TemplateResponse"
    - from: "app/templates/calendar.html"
      to: "tests/test_calendar_views.py"
      via: "Locale formatting markers asserted in rendered HTML"
      pattern: "toLocale|Intl.DateTimeFormat|pl-PL"
---

<objective>
Finalize Phase 8 by enforcing Polish-first locale formatting for date/time outputs and adding regression coverage.

Purpose: ensure localization is not only translated text but also culturally correct temporal display behavior.
Output: locale-aware date/time rendering updates and tests validating Polish defaults.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-localization-foundation-and-polish-default/08-CONTEXT.md
@.planning/phases/08-localization-foundation-and-polish-default/08-RESEARCH.md
@.planning/phases/08-localization-foundation-and-polish-default/08-02-SUMMARY.md
@app/views/calendar_routes.py
@app/templates/calendar.html
@app/templates/partials/month_grid.html
@app/templates/partials/day_events.html
@tests/test_calendar_views.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace locale-formatting shortcuts with Polish-default locale strategy</name>
  <files>app/views/calendar_routes.py, app/templates/calendar.html, app/templates/partials/month_grid.html, app/templates/partials/day_events.html</files>
  <behavior>
    - Month/day and datetime display use locale mapping based on resolved app locale (`pl` default).
    - Hardcoded `calendar.month_name` and `sv-SE` formatting shortcuts are removed from user-visible outputs.
    - Existing day/month navigation and quick-add review flows stay functional.
  </behavior>
  <action>Introduce consistent locale formatting helpers for server-provided month/day labels and inline JavaScript date/time rendering. Ensure default locale path renders Polish conventions while preserving stable machine-friendly values for API requests.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_calendar_views.py -k "month or day or quick_add"</automated>
  </verify>
  <done>All date/time text shown in Phase 8 surfaces follows Polish conventions by default without workflow regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add localization regression coverage for Polish default rendering</name>
  <files>tests/test_calendar_views.py, tests/test_events_api.py</files>
  <behavior>
    - Tests validate that core rendered pages include Polish-default labels/markers.
    - Tests verify locale-aware formatting helpers are wired in rendered HTML or route outputs.
    - Existing quick-add and calendar behavior tests continue to pass.
  </behavior>
  <action>Extend integration tests to assert Polish-default output markers across calendar shell, month/day partials, and representative API-rendered text. Keep assertions robust (stable markers, not brittle full-sentence snapshots).</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_calendar_views.py tests/test_events_api.py</automated>
  </verify>
  <done>Automated tests catch regressions in Polish default locale rendering and formatting behavior.</done>
</task>

</tasks>

<verification>
Run `.venv\Scripts\python.exe -m pytest -q tests/test_calendar_views.py tests/test_events_api.py tests/test_auth.py`.
</verification>

<success_criteria>
- Date/time outputs use Polish conventions in default mode.
- English/Swedish hardcoded formatting shortcuts are removed from user-facing display paths.
- Regression suite covers Polish default localization behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/08-localization-foundation-and-polish-default/08-03-SUMMARY.md`
</output>
