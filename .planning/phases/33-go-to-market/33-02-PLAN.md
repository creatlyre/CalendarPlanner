---
phase: 33-go-to-market
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/landing.html
  - app/templates/terms.html
  - app/templates/privacy.html
  - app/templates/refund.html
  - app/templates/base.html
  - app/views/__init__.py
  - app/locales/en.json
  - app/locales/pl.json
  - main.py
autonomous: true
requirements:
  - GTM-02
  - GTM-04

must_haves:
  truths:
    - "Unauthenticated user visiting / sees a landing page with hero, features, pricing section, and CTA"
    - "Authenticated user visiting / is redirected to /dashboard (existing behavior preserved)"
    - "User can visit /terms, /privacy, and /refund to see legal pages"
    - "Legal pages are bilingual (PL/EN) and GDPR-compliant"
    - "Footer on all app pages links to terms, privacy, and refund"
  artifacts:
    - path: "app/templates/landing.html"
      provides: "Landing page with hero, features, pricing, CTA"
      min_lines: 100
    - path: "app/templates/terms.html"
      provides: "Terms of Service page"
      min_lines: 40
    - path: "app/templates/privacy.html"
      provides: "Privacy Policy page"
      min_lines: 40
    - path: "app/templates/refund.html"
      provides: "Refund/Cancellation Policy page"
      min_lines: 30
    - path: "app/templates/base.html"
      provides: "Footer with legal page links"
      contains: "/terms"
  key_links:
    - from: "main.py"
      to: "app/templates/landing.html"
      via: "root route for unauthenticated users"
      pattern: "landing\\.html"
    - from: "app/templates/base.html"
      to: "/terms, /privacy, /refund"
      via: "footer links"
      pattern: "href.*terms|privacy|refund"
---

<objective>
Landing page for unauthenticated visitors and three legal pages (terms, privacy, refund) with footer integration across all app pages.

Purpose: Convert visitors into signups with a compelling landing page. Meet legal requirements for a commercial SaaS with GDPR-compliant legal pages.
Output: Landing page at `/`, legal pages at `/terms`, `/privacy`, `/refund`, footer links on all pages.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-go-to-market/33-CONTEXT.md

@app/templates/base.html
@app/templates/dashboard.html
@main.py
@app/i18n.py
@app/locales/en.json
@app/locales/pl.json
@MONETIZATION.md
@COMMERCIAL-LICENSE.md

<interfaces>
<!-- Key patterns the executor needs -->

From main.py:
```python
# Current root route — requires auth, redirects to dashboard
@app.get("/", response_class=HTMLResponse)
async def root(request: Request, user=Depends(get_current_user)):
    return RedirectResponse(url="/dashboard", status_code=302)

# Auth redirect handler — 401 on /, /invite, /dashboard → redirect to /auth/login
@app.exception_handler(HTTPException)
async def auth_redirect_handler(request: Request, exc: HTTPException):
    if exc.status_code == 401 and request.url.path in {"/", "/invite", "/dashboard"}:
        return RedirectResponse(url="/auth/login", status_code=307)
```

From app/i18n.py:
```python
def inject_template_i18n(request, context_dict) -> dict:
    # Adds t() translation function to template context
def set_locale_cookie_if_param(response, request):
    # Sets locale cookie from ?lang= query param
```

From app/templates/base.html:
```html
<!-- Dark theme template with nav, content block, footer -->
<!-- Uses Tailwind classes, bg-[#0f0a2e] -->
{% block content %}{% endblock %}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create landing page with hero, features, pricing, and CTA sections</name>
  <files>app/templates/landing.html, main.py, app/locales/en.json, app/locales/pl.json</files>
  <action>
1. **app/templates/landing.html** — Create standalone landing page (does NOT extend base.html — it has its own minimal structure to avoid nav distractions):
   - **HTML structure**: Full page with `<html>`, `<head>` (meta viewport, title "Synco", Tailwind CDN link matching app's approach, favicon), `<body>` with dark theme bg-[#0f0a2e]
   - **Top bar**: Logo "Synco" on left, "Zaloguj się" / "Sign in" link on right pointing to `/auth/login`. NO full nav menu (remove distractions per landing page best practices)
   - **Hero section**: 
     - Headline: `{{ t('landing_hero_title') }}` → "Jeden kalendarz dla dwojga" (PL) / "One calendar for two" (EN)
     - Subtitle: `{{ t('landing_hero_subtitle') }}` → "Wspólny kalendarz, budżet i lista zakupów dla par i rodzin" / "Shared calendar, budget and shopping list for couples and families"
     - Primary CTA button: `{{ t('landing_hero_cta') }}` → "Zacznij za darmo" / "Start for free" → links to `/auth/login`
     - Secondary link: `{{ t('landing_hero_secondary') }}` → "Zobacz cennik" / "See pricing" → scrolls to #pricing
     - Right side or below: CSS-only mockup of the app (a rounded rectangle with some colored calendar blocks to suggest the UI) OR a simple gradient illustration. Do NOT use an external image. Keep it pure CSS/SVG
   - **Features section** (id="features"): 
     - Section title: `{{ t('landing_features_title') }}` → "Wszystko, czego potrzebujecie" / "Everything you need"
     - 6 feature cards in 2x3 grid (1 col mobile, 2 col md, 3 col lg):
       1. 📅 Shared Calendar — sync events, Google Calendar integration
       2. 💰 Budget Tracker — income, expenses, categories, charts
       3. 🛒 Shopping List — shared lists, real-time sync
       4. 🔔 Notifications — email reminders, event alerts
       5. 🔄 Google Sync — two-way Google Calendar synchronization
       6. 📱 Works Everywhere — PWA, mobile-friendly, install on phone
     - Each card: emoji icon, title, short description. Dark card background (bg-[#1a1444]), rounded-xl, subtle border
   - **How It Works section**: 
     - 3 steps in a horizontal flow: 
       1. "Załóż konto" / "Create account" — person icon
       2. "Zaproś partnera" / "Invite partner" — link icon  
       3. "Planujcie razem" / "Plan together" — calendar icon
     - Connected with a horizontal line/arrow between steps
   - **Pricing section** (id="pricing"):
     - Same pricing content as Plan 01's /pricing page but embedded. To avoid duplication, include a simplified version with just the 3 SaaS tier cards (Free 0 zł, Pro 19 zł, Family Plus 29 zł) and a "Self-hosted option available" link to `/pricing`
     - Each card with name, price, 3-4 key features, CTA button
     - Pro card highlighted with "Recommended" badge
   - **Footer**: 
     - Legal links: `/terms`, `/privacy`, `/refund`
     - "Made with ❤️ for couples" / "Stworzone z ❤️ dla par"
     - Language toggle (PL/EN)
   - **Styling**: Dark theme consistent with app. Use Tailwind utility classes. Smooth scroll behavior for anchor links

2. **main.py** — Modify root route to serve landing page for unauthenticated users:
   - Change the `@app.get("/")` handler:
     ```python
     @app.get("/", response_class=HTMLResponse)
     async def root(request: Request):
         # Try to get current user — if authenticated, redirect to dashboard
         from app.auth.dependencies import get_current_user_optional
         # If get_current_user_optional doesn't exist, implement inline:
         # Check for session token in cookie, validate, if valid → redirect to /dashboard
         # If not authenticated → render landing.html
     ```
   - The key change: `/` no longer uses `Depends(get_current_user)` which throws 401. Instead, it optionally checks auth:
     - If user has valid session → `RedirectResponse(url="/dashboard", status_code=302)`
     - If no session → render `landing.html` with i18n context
   - Update the `auth_redirect_handler` exception handler to remove `/` from the 401-redirect paths (since `/` no longer raises 401)
   - If `get_current_user_optional` doesn't exist in auth dependencies, create it: same as `get_current_user` but returns `None` instead of raising `HTTPException(401)` when no valid session

3. **app/locales/en.json** and **app/locales/pl.json** — Add all landing page i18n keys:
   - `landing_hero_title`, `landing_hero_subtitle`, `landing_hero_cta`, `landing_hero_secondary`
   - `landing_features_title`, `landing_feature_calendar_title`, `landing_feature_calendar_desc`, etc. (6 features × 2 keys each)
   - `landing_how_it_works_title`, `landing_step_1_title`, `landing_step_1_desc`, etc.
   - `landing_pricing_title`, `landing_footer_made_with`
  </action>
  <verify>
    <automated>cd c:\Users\Wojciech\Documents\Python\CalendarPlanner ; python -c "from main import app; routes = [r.path for r in app.routes]; print('Routes:', [r for r in routes if r == '/']); assert '/' in routes, 'Missing / route'" ; python -c "import json; en = json.load(open('app/locales/en.json')); assert 'landing_hero_title' in en, 'Missing landing i18n'; print('Landing i18n keys present')" ; python -c "import os; assert os.path.exists('app/templates/landing.html'), 'Missing landing template'; print('landing.html exists')"</automated>
  </verify>
  <done>Landing page renders at / for unauthenticated users with hero (headline + CTA), 6 feature cards, 3-step how-it-works, embedded pricing section, and footer. Authenticated users still redirect to /dashboard. Page works in PL and EN.</done>
</task>

<task type="auto">
  <name>Task 2: Create legal pages (terms, privacy, refund) and add footer links</name>
  <files>app/templates/terms.html, app/templates/privacy.html, app/templates/refund.html, app/templates/base.html, main.py, app/locales/en.json, app/locales/pl.json</files>
  <action>
1. **Legal page templates** — Create 3 templates extending base.html. Each is a simple content page with legal text:

   **app/templates/terms.html** — Terms of Service:
   - Extends base.html
   - Heading: "Terms of Service" / "Regulamin"
   - Sections (content via i18n keys for bilingual):
     - Introduction: What Synco is, who operates it
     - Account Terms: User must be 16+, responsible for account security, one account per person
     - Service Description: Shared calendar, budget, shopping list for households
     - Plans and Billing: Free tier, paid plans via Stripe, billing cycle, plan changes
     - Intellectual Property: AGPL-3.0 for source code, Synco brand trademarks reserved
     - Acceptable Use: No illegal activity, no automated scraping, no abuse of free tier
     - Limitation of Liability: Service provided as-is, no guarantee of uptime beyond reasonable efforts
     - Termination: User can delete account anytime, we can suspend for TOS violation
     - Changes to Terms: 30-day notice for material changes via email
     - Governing Law: Polish law (Civil Code), disputes resolved in Polish courts
     - Contact: support email

   **app/templates/privacy.html** — Privacy Policy:
   - Extends base.html
   - Heading: "Privacy Policy" / "Polityka Prywatności"
   - Sections:
     - Data Controller: Developer name, contact email
     - Data We Collect: Account data (email, name — from Google OAuth), calendar events, budget data, shopping lists. No data collected beyond what user enters
     - Legal Basis (GDPR Art. 6): Contract performance for account and app data, legitimate interest for security/logging, consent for optional email notifications
     - How We Use Data: Provide the service, sync with Google Calendar (user-initiated), send notifications (if enabled), billing via Stripe
     - Data Sharing: Stripe for payments (name, email), Google for Calendar sync (user-authorized OAuth). No data sold to third parties. No advertising partners
     - Cookies: Session cookie for authentication only. No tracking cookies, no third-party analytics cookies
     - Data Retention: Account data kept while account active. Deleted within 30 days of account deletion. Billing records retained for 5 years (tax obligations)
     - Your Rights (GDPR): Access, rectification, erasure, portability, restriction, objection. Contact email to exercise rights. Response within 30 days
     - Data Security: Encrypted in transit (TLS), Supabase-managed database encryption at rest, no plain-text password storage (OAuth only)
     - International Transfers: Data processed within EU (Supabase EU region). Google Calendar sync subject to Google's privacy policy
     - Children: Service not directed at children under 16
     - Changes: 30-day notice for material changes
     - Contact: Email for privacy inquiries

   **app/templates/refund.html** — Refund & Cancellation Policy:
   - Extends base.html
   - Heading: "Refund & Cancellation Policy" / "Polityka Zwrotów i Anulowania"
   - Sections:
     - Free Tier: Always free, no payment needed
     - SaaS Subscriptions: 14-day money-back guarantee (EU consumer right). Cancel anytime via Stripe Customer Portal. Access continues until end of billing period. Refund requests via email within 14 days of purchase
     - Annual Subscriptions: Same 14-day refund window from purchase date. No pro-rata refunds after 14 days
     - Self-Hosted License: Non-refundable after license key delivery (digital good, immediate delivery per EU Directive 2011/83). Buyer can evaluate via free tier before purchase
     - How to Cancel: Click "Manage subscription" in billing settings → Stripe Customer Portal → Cancel
     - How to Request Refund: Email with order details within 14 days
     - Contact: Support email

2. **Register legal page routes in main.py** — Add public routes (no auth required):
   ```python
   @app.get("/terms", response_class=HTMLResponse)
   async def terms_page(request: Request):
       context = inject_template_i18n(request, {"request": request})
       response = templates.TemplateResponse(request=request, name="terms.html", context=context)
       set_locale_cookie_if_param(response, request)
       return response
   ```
   Same pattern for `/privacy` and `/refund`. These go directly in main.py since they're simple standalone pages (no module-specific logic). Add them after the root route.

3. **app/templates/base.html** — Add legal links to footer:
   - In the footer section of base.html, add 3 links:
     - `<a href="/terms">{{ t('footer_terms') }}</a>`
     - `<a href="/privacy">{{ t('footer_privacy') }}</a>`
     - `<a href="/refund">{{ t('footer_refund') }}</a>`
   - Style: text-gray-400, hover:text-white, separated by `·` or `|`

4. **app/locales/en.json** and **app/locales/pl.json** — Add all legal page i18n keys:
   - Footer: `footer_terms`, `footer_privacy`, `footer_refund`
   - Terms page: `terms_title`, `terms_intro`, `terms_account`, `terms_service`, `terms_billing`, `terms_ip`, `terms_acceptable_use`, `terms_liability`, `terms_termination`, `terms_changes`, `terms_governing_law`, `terms_contact`
   - Privacy page: `privacy_title`, `privacy_controller`, `privacy_data_collected`, `privacy_legal_basis`, `privacy_usage`, `privacy_sharing`, `privacy_cookies`, `privacy_retention`, `privacy_rights`, `privacy_security`, `privacy_transfers`, `privacy_children`, `privacy_changes`, `privacy_contact`
   - Refund page: `refund_title`, `refund_free_tier`, `refund_saas`, `refund_annual`, `refund_self_hosted`, `refund_how_to_cancel`, `refund_how_to_request`, `refund_contact`
   - Polish translations should be legally appropriate for the Polish market
  </action>
  <verify>
    <automated>cd c:\Users\Wojciech\Documents\Python\CalendarPlanner ; python -c "from main import app; routes = [r.path for r in app.routes]; missing = [p for p in ['/terms', '/privacy', '/refund'] if p not in routes]; assert not missing, f'Missing routes: {missing}'; print('All legal routes registered')" ; python -c "import os; files = ['app/templates/terms.html', 'app/templates/privacy.html', 'app/templates/refund.html']; missing = [f for f in files if not os.path.exists(f)]; assert not missing, f'Missing: {missing}'; print('All legal templates exist')" ; python -c "import json; en = json.load(open('app/locales/en.json')); assert 'footer_terms' in en and 'terms_title' in en and 'privacy_title' in en and 'refund_title' in en, 'Missing i18n keys'; print('Legal i18n keys present')"</automated>
  </verify>
  <done>Three legal pages render at /terms, /privacy, /refund with bilingual content. Base template footer includes legal page links on all app pages. All i18n keys for both PL and EN are present. Privacy policy is GDPR-compliant with data subject rights section.</done>
</task>

</tasks>

<verification>
1. Visit `/` without auth — landing page renders with hero, features, pricing, footer
2. Visit `/` with auth — redirects to /dashboard
3. Visit `/terms`, `/privacy`, `/refund` — legal pages render with proper content
4. Switch locale to PL → all pages show Polish text
5. Legal page links visible in footer of authenticated pages (dashboard, calendar, etc.)
6. All existing tests pass: `pytest tests/ -x`
</verification>

<success_criteria>
- Landing page at `/` for unauthenticated users with hero section, 6 feature cards, 3-step flow, pricing preview, and CTA
- Authenticated users still redirected to `/dashboard` from `/`
- Three legal pages accessible at `/terms`, `/privacy`, `/refund`
- Privacy policy includes GDPR data subject rights and legal basis
- Refund policy states 14-day money-back for SaaS, non-refundable for self-hosted license
- Footer links to legal pages present on all app pages via base.html
- All content bilingual (PL/EN) via i18n system
</success_criteria>

<output>
After completion, create `.planning/phases/33-go-to-market/33-02-SUMMARY.md`
</output>
