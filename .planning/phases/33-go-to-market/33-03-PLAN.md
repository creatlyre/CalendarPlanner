---
phase: 33-go-to-market
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/docker-publish.yml
  - Dockerfile
  - self-hosted/docker-compose.yml
  - LAUNCH.md
autonomous: true
requirements:
  - GTM-05

must_haves:
  truths:
    - "Pushing a git tag matching v* triggers a GitHub Actions workflow that builds and pushes a Docker image to GHCR"
    - "Self-hosted docker-compose.yml references the GHCR image instead of local build"
    - "Dockerfile has OCI labels for GHCR metadata (source, description, licenses)"
    - "LAUNCH.md exists with pre-launch checklist, day-1 actions, and 30-day success criteria"
  artifacts:
    - path: ".github/workflows/docker-publish.yml"
      provides: "CI/CD workflow for Docker image publishing"
      min_lines: 30
    - path: "Dockerfile"
      provides: "OCI labels for GHCR"
      contains: "org.opencontainers.image"
    - path: "self-hosted/docker-compose.yml"
      provides: "GHCR image reference"
      contains: "ghcr.io"
    - path: "LAUNCH.md"
      provides: "Launch checklist with success criteria"
      min_lines: 50
  key_links:
    - from: ".github/workflows/docker-publish.yml"
      to: "Dockerfile"
      via: "docker build and push"
      pattern: "docker.*build|push"
    - from: "self-hosted/docker-compose.yml"
      to: "ghcr.io"
      via: "image reference"
      pattern: "image.*ghcr\\.io"
---

<objective>
GitHub Actions CI/CD workflow for publishing Docker images to GHCR on release tags, Dockerfile OCI labels, docker-compose update to use published image, and comprehensive launch checklist.

Purpose: Self-hosted buyers need a published Docker image to pull. Launch checklist ensures nothing is missed before going live.
Output: `.github/workflows/docker-publish.yml`, updated Dockerfile and docker-compose.yml, `LAUNCH.md`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-go-to-market/33-CONTEXT.md

@Dockerfile
@self-hosted/docker-compose.yml
@self-hosted/README.md
@MONETIZATION.md
@COMMERCIAL-LICENSE.md
@config.py

<interfaces>
<!-- Key files the executor needs to understand -->

From Dockerfile:
```dockerfile
# Multi-stage build: builder (python:3.12-slim) → runtime
# No OCI labels currently
# CMD: gunicorn with uvicorn workers
# HEALTHCHECK: python urllib check on /health/ready
```

From self-hosted/docker-compose.yml:
```yaml
services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile
    # Currently builds from local source — needs to reference GHCR image
```

From config.py:
```python
ENVIRONMENT: str = "development"
SYNCO_LICENSE_KEY: str = ""
SYNCO_LICENSE_SECRET: str = ""
# License check middleware is already in place
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions workflow and update Dockerfile with OCI labels</name>
  <files>.github/workflows/docker-publish.yml, Dockerfile</files>
  <action>
1. **Dockerfile** — Add OCI labels after the `FROM python:3.12-slim AS runtime` line:
   ```dockerfile
   LABEL org.opencontainers.image.source="https://github.com/OWNER/synco"
   LABEL org.opencontainers.image.description="Synco – shared household calendar, budget & shopping list"
   LABEL org.opencontainers.image.licenses="AGPL-3.0-only"
   LABEL org.opencontainers.image.title="Synco"
   ```
   Use `OWNER` as a placeholder — the user will replace with their GitHub username/org. Add a comment `# TODO: Replace OWNER with your GitHub username` above the source label.

2. **.github/workflows/docker-publish.yml** — Create GitHub Actions workflow:
   ```yaml
   name: Publish Docker Image

   on:
     push:
       tags:
         - 'v*'

   env:
     REGISTRY: ghcr.io
     IMAGE_NAME: ${{ github.repository }}

   jobs:
     build-and-push:
       runs-on: ubuntu-latest
       permissions:
         contents: read
         packages: write

       steps:
         - name: Checkout repository
           uses: actions/checkout@v4

         - name: Log in to Container Registry
           uses: docker/login-action@v3
           with:
             registry: ${{ env.REGISTRY }}
             username: ${{ github.actor }}
             password: ${{ secrets.GITHUB_TOKEN }}

         - name: Extract metadata (tags, labels)
           id: meta
           uses: docker/metadata-action@v5
           with:
             images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
             tags: |
               type=semver,pattern={{version}}
               type=semver,pattern={{major}}.{{minor}}
               type=semver,pattern={{major}}
               type=raw,value=latest,enable={{is_default_branch}}

         - name: Set up Docker Buildx
           uses: docker/setup-buildx-action@v3

         - name: Build and push Docker image
           uses: docker/build-push-action@v6
           with:
             context: .
             push: true
             tags: ${{ steps.meta.outputs.tags }}
             labels: ${{ steps.meta.outputs.labels }}
             cache-from: type=gha
             cache-to: type=gha,mode=max
   ```

   Key design decisions:
   - Uses `GITHUB_TOKEN` (no separate credentials needed)
   - `docker/metadata-action` auto-generates semantic version tags (e.g., v4.0.0 → 4.0.0, 4.0, 4, latest)
   - BuildKit GHA cache for faster subsequent builds
   - linux/amd64 only (no explicit platform — default is runner's arch)
   - `permissions: packages: write` is required for GHCR
  </action>
  <verify>
    <automated>cd c:\Users\Wojciech\Documents\Python\CalendarPlanner ; python -c "import os; assert os.path.exists('.github/workflows/docker-publish.yml'), 'Missing workflow'; print('Workflow exists')" ; python -c "f = open('Dockerfile').read(); assert 'org.opencontainers.image' in f, 'Missing OCI labels'; print('OCI labels present')" ; python -c "import yaml; w = yaml.safe_load(open('.github/workflows/docker-publish.yml')); assert 'push' in w.get('on', {}), 'Missing push trigger'; assert 'tags' in w['on']['push'], 'Missing tag trigger'; print('Workflow structure valid')"</automated>
  </verify>
  <done>GitHub Actions workflow at .github/workflows/docker-publish.yml triggers on v* tags, builds Docker image, and pushes to GHCR with semantic version tags. Dockerfile has OCI labels for GHCR metadata display.</done>
</task>

<task type="auto">
  <name>Task 2: Update docker-compose for GHCR image and create LAUNCH.md</name>
  <files>self-hosted/docker-compose.yml, self-hosted/README.md, LAUNCH.md</files>
  <action>
1. **self-hosted/docker-compose.yml** — Replace the `build` directive with `image` reference:
   ```yaml
   services:
     app:
       image: ghcr.io/OWNER/synco:latest
       # Remove the build: section entirely
       env_file: .env
       environment:
         - ENVIRONMENT=self-hosted
         - SUPABASE_URL=http://rest:3000
       # ... rest stays the same
   ```
   Add comment: `# TODO: Replace OWNER with the GitHub username/org`
   Keep all other services (db, rest, caddy) unchanged.

2. **self-hosted/README.md** — Update any references to "docker-compose build" or local build instructions. Add a note that the image is pulled from GHCR: `docker pull ghcr.io/OWNER/synco:latest`. If the README mentions building from source, update to note that's only for development; production uses the published image.

3. **LAUNCH.md** — Create comprehensive launch checklist:

   ```markdown
   # Synco Launch Checklist

   ## Pre-Launch (Before Going Live)

   ### Infrastructure
   - [ ] Railway deployment working (health check green)
   - [ ] Custom domain configured in Railway dashboard
   - [ ] HTTPS/SSL active on custom domain
   - [ ] Environment variables set in Railway:
     - [ ] SUPABASE_URL, SUPABASE_KEY
     - [ ] GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET
     - [ ] STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET
     - [ ] STRIPE_PRO_PRICE_ID, STRIPE_PRO_ANNUAL_PRICE_ID
     - [ ] STRIPE_FAMILY_PLUS_PRICE_ID, STRIPE_FAMILY_PLUS_ANNUAL_PRICE_ID
     - [ ] STRIPE_SELF_HOSTED_PRICE_ID
     - [ ] SENTRY_DSN
     - [ ] ENVIRONMENT=production
     - [ ] ALLOWED_ORIGINS (production domain)

   ### Stripe
   - [ ] Stripe account activated (not test mode)
   - [ ] Products created: Pro Monthly, Pro Annual, Family Plus Monthly, Family Plus Annual, Self-Hosted License
   - [ ] Price IDs copied to Railway env vars
   - [ ] Webhook endpoint configured: https://yourdomain.com/api/billing/webhook
   - [ ] Webhook events enabled: checkout.session.completed, customer.subscription.updated, customer.subscription.deleted, invoice.payment_failed
   - [ ] Customer Portal configured (cancellation, plan changes)
   - [ ] Test checkout flow end-to-end with Stripe test cards

   ### Google OAuth
   - [ ] Google Cloud Console project configured for production
   - [ ] OAuth consent screen submitted for verification (if >100 users)
   - [ ] Redirect URI updated to production domain

   ### Content
   - [ ] Landing page loads at / (unauthenticated)
   - [ ] Pricing page loads at /pricing
   - [ ] Legal pages load: /terms, /privacy, /refund
   - [ ] All pages work in Polish and English
   - [ ] Footer links present on all authenticated pages

   ### Self-Hosted
   - [ ] Docker image published to GHCR (`docker pull ghcr.io/OWNER/synco:latest`)
   - [ ] License key generation tested
   - [ ] Self-hosted docker-compose.yml works with GHCR image
   - [ ] Setup guide accurate and tested

   ### Testing
   - [ ] All automated tests passing (`pytest tests/ -x`)
   - [ ] Manual smoke test: sign up → create event → sync → budget → shopping list
   - [ ] Mobile responsive check on phone
   - [ ] PWA install works (manifest + service worker)

   ## Day 1 Actions

   - [ ] Push production tag (e.g., `git tag v4.0.0 && git push --tags`)
   - [ ] Verify Docker image published to GHCR
   - [ ] Monitor Sentry for errors (first 24 hours)
   - [ ] Monitor Stripe webhook delivery in Stripe Dashboard
   - [ ] Check Railway logs for any startup errors
   - [ ] Test signup flow from landing page to dashboard
   - [ ] Test checkout flow for Pro plan

   ## 30-Day Success Criteria

   | Metric | Target | How to Measure |
   |--------|--------|---------------|
   | Paying subscriber | ≥ 1 | Stripe Dashboard → Subscriptions |
   | Self-hosted purchase | ≥ 1 | Stripe Dashboard → Payments (one-time) |
   | Critical bugs | 0 | Sentry error count (critical/fatal) |
   | PWA installable | Yes | Test on Android Chrome + Desktop Chrome |
   | Uptime | > 99% | Railway metrics / health endpoint monitoring |

   ## Post-Launch Monitoring

   ### Daily (First Week)
   - Check Sentry for new errors
   - Check Stripe webhook delivery status
   - Review Railway deployment logs

   ### Weekly (First Month)
   - Review Stripe MRR and subscriber count
   - Check Google OAuth quota usage
   - Review error trends in Sentry

   ### Monthly (Ongoing)
   - Review churn rate in Stripe
   - Update dependencies (security patches)
   - Review and respond to user feedback
   ```
  </action>
  <verify>
    <automated>cd c:\Users\Wojciech\Documents\Python\CalendarPlanner ; python -c "import os; assert os.path.exists('LAUNCH.md'), 'Missing LAUNCH.md'; print('LAUNCH.md exists')" ; python -c "f = open('self-hosted/docker-compose.yml').read(); assert 'ghcr.io' in f, 'Missing GHCR reference'; assert 'build:' not in f.split('app:')[1].split('env_file')[0] if 'build:' not in f else True, 'Build directive should be removed'; print('docker-compose updated')" ; python -c "f = open('LAUNCH.md').read(); assert '30-Day' in f, 'Missing success criteria'; assert 'Stripe' in f, 'Missing Stripe checklist'; print('LAUNCH.md has required sections')"</automated>
  </verify>
  <done>Self-hosted docker-compose.yml references ghcr.io image instead of local build. LAUNCH.md contains complete pre-launch checklist (infrastructure, Stripe, Google OAuth, content, self-hosted, testing), day-1 actions, 30-day success criteria table, and post-launch monitoring schedule.</done>
</task>

</tasks>

<verification>
1. `.github/workflows/docker-publish.yml` exists with correct trigger (v* tags) and GHCR push steps
2. Dockerfile has OCI labels (org.opencontainers.image.source, description, licenses)
3. `self-hosted/docker-compose.yml` uses `image: ghcr.io/OWNER/synco:latest` instead of build
4. `LAUNCH.md` exists with all checklist sections
5. YAML workflow file is valid (can be parsed)
</verification>

<success_criteria>
- GitHub Actions workflow triggers on v* tag push and publishes to GHCR
- Docker image tagged with semantic version (e.g., 4.0.0, 4.0, 4, latest)
- Workflow uses GITHUB_TOKEN for auth (no separate secrets needed)
- Self-hosted docker-compose references published GHCR image
- LAUNCH.md has pre-launch checklist, day-1 actions, and 30-day success metrics
- All self-hosted docs updated to reference GHCR image
</success_criteria>

<output>
After completion, create `.planning/phases/33-go-to-market/33-03-SUMMARY.md`
</output>
