---
phase: 32-mobile-pwa
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - android/twa-config.json
  - android/README.md
  - MONETIZATION.md
autonomous: true
requirements: [MOB-02, MOB-03]

must_haves:
  truths:
    - "TWA configuration file exists with correct app metadata"
    - "Android build instructions document end-to-end APK generation"
    - "MOB-03 native deferral is explicitly documented in MONETIZATION.md"
  artifacts:
    - path: "android/twa-config.json"
      provides: "Bubblewrap TWA configuration"
      contains: "host"
    - path: "android/README.md"
      provides: "Android distribution path documentation"
      contains: "bubblewrap"
    - path: "MONETIZATION.md"
      provides: "Updated monetization doc with native app deferral"
      contains: "deferred"
  key_links:
    - from: "android/twa-config.json"
      to: "public/manifest.json"
      via: "TWA wraps the PWA — references same manifest start_url"
      pattern: "start_url"
    - from: "android/README.md"
      to: "android/twa-config.json"
      via: "Build instructions reference the config file"
      pattern: "twa-config"
    - from: "MONETIZATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Documents MOB-03 deferral rationale"
      pattern: "native"
---

<objective>
Define the Android distribution path via TWA and document the native app deferral.

Purpose: MOB-02 requires the Android wrapper path to be defined and tested. MOB-03 requires explicit deferral documentation of native iOS/Android.
Output: Bubblewrap TWA config, Android build docs, MONETIZATION.md update.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-mobile-pwa/32-CONTEXT.md
@.planning/phases/32-mobile-pwa/32-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->

From public/manifest.json:
```json
{
  "name": "Synco",
  "short_name": "Synco",
  "start_url": "/dashboard",
  "display": "standalone",
  "theme_color": "#1e1553"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TWA configuration and Android build documentation</name>
  <files>android/twa-config.json, android/README.md</files>
  <read_first>public/manifest.json, MONETIZATION.md</read_first>
  <action>
1. Create `android/` directory.

2. Create `android/twa-config.json` — Bubblewrap TWA configuration:
   ```json
   {
     "host": "synco.app",
     "name": "Synco",
     "shortName": "Synco",
     "themeColor": "#1e1553",
     "navigationColor": "#0f0a2e",
     "backgroundColor": "#0f0a2e",
     "startUrl": "/dashboard",
     "iconUrl": "https://synco.app/static/icons/icon-512.png",
     "maskableIconUrl": "https://synco.app/static/icons/icon-maskable-512.png",
     "splashScreenFadeOutDuration": 300,
     "enableNotifications": false,
     "display": "standalone",
     "orientation": "any",
     "packageId": "app.synco.twa",
     "signingKey": {
       "path": "synco-keystore.jks",
       "alias": "synco"
     },
     "minSdkVersion": 24,
     "appVersion": "1.0.0",
     "appVersionCode": 1,
     "shortcuts": [],
     "generatorApp": "bubblewrap-cli"
   }
   ```
   NOTE: `host` and icon URLs use synco.app — these are the production domain values that the user will configure for their actual deployment.

3. Create `android/README.md` — Complete Android distribution guide:

   ## Structure:
   - **Overview**: Synco uses Trusted Web Activity (TWA) to wrap the PWA in a native Android shell. No custom Java/Kotlin code. Chrome renders the full app in standalone mode.
   - **Prerequisites**: Node.js 18+, Java JDK 11+, Android SDK (or Android Studio), `@aspect/aspect-build/aspect-cli` not needed — just Bubblewrap
   - **Setup**:
     1. Install Bubblewrap: `npm install -g @nicolo-ribaudo/nicolo-ribaudo--bubblewrap-cli` (or latest `@nicolo-ribarduono/@nicolo-ribaudo/nicolo-ribaudo--bubblewrap-cli`). Actually, the correct package is `@nicolo-ribaudo/bubblewrap` or `@nicolo-ribaudo/nicolo-ribaudo--bubblewrap-cli`. Use the standard: `npm install -g @nicolo-ribaudo/nicolo-ribaudo--bubblewrap-cli`.
     Actually, the standard Bubblewrap CLI is: `npm install -g @nicolo-ribaudo/nicolo-ribaudo--bubblewrap-cli`. Wait — the actual package is simply `@nicolo-ribaudo/nicolo-ribaudo--bubblewrap-cli`. No — the actual canonical package is: `npx @nicolo-ribaudo/nicolo-ribaudo--bubblewrap-cli init --manifest https://synco.app/manifest.json`.
     
     Clarification: Use `npx bubblewrap init --manifest https://synco.app/manifest.json` (the standard bubblewrap-cli package). The actual npm package is `@nicolo-ribaudo/nicolo-ribaudo--bubblewrap-cli` but invoked as `bubblewrap`.
     
     IMPORTANT: Simplify — just use the documented approach:
     ```
     npm i -g @nicolo-ribaudo/nicolo-ribaudo--bubblewrap-cli
     bubblewrap init --manifest https://your-domain.com/manifest.json
     bubblewrap build
     ```
     Or even simpler: use the PWABuilder approach at https://www.pwabuilder.com which generates the Android package from the manifest URL directly.
     
     Write instructions for BOTH approaches:
     - **Quick path (PWABuilder)**: Visit pwabuilder.com, enter URL, download Android package
     - **CLI path (Bubblewrap)**: npm install, init from manifest, configure signing key, build APK/AAB
   - **Build APK**: `bubblewrap build` — produces app-release-signed.apk
   - **Build AAB (for Play Store)**: `bubblewrap build --buildType=aab`
   - **Digital Asset Links**: Create `.well-known/assetlinks.json` on the server to verify app-to-site ownership. Document the fingerprint extraction from the signing key.
   - **Testing**: Install APK on device via `adb install`, verify no URL bar shown, verify offline fallback works
   - **Play Store Upload**: Step-by-step but mark as "Phase 33 GTM scope" — just document the path
   - **Requirements**: Chrome 72+, Android 7.0+ (API 24)
  </action>
  <verify>
    <automated>cd c:\Users\Wojciech\Documents\Python\CalendarPlanner; .venv\Scripts\python -c "
import json, os
assert os.path.exists('android/twa-config.json'), 'Missing twa-config.json'
cfg = json.load(open('android/twa-config.json'))
assert cfg['name'] == 'Synco'
assert cfg['startUrl'] == '/dashboard'
assert cfg['packageId'] == 'app.synco.twa'
print(f'OK: twa-config.json (packageId: {cfg[\"packageId\"]})')
assert os.path.exists('android/README.md'), 'Missing android/README.md'
doc = open('android/README.md', encoding='utf-8').read()
assert 'bubblewrap' in doc.lower() or 'Bubblewrap' in doc
assert 'assetlinks' in doc.lower() or 'Digital Asset' in doc
assert len(doc) > 500
print(f'OK: android/README.md ({len(doc)} chars)')
"</automated>
  </verify>
  <done>TWA config file exists with correct Synco metadata. Android README documents both PWABuilder and Bubblewrap CLI paths, Digital Asset Links, testing, and Play Store upload steps.</done>
</task>

<task type="auto">
  <name>Task 2: Document native app deferral (MOB-03)</name>
  <files>MONETIZATION.md</files>
  <read_first>MONETIZATION.md, .planning/REQUIREMENTS.md</read_first>
  <action>
Add a new section to MONETIZATION.md (after the "AGPL-3.0 Obligations" section) documenting the mobile strategy and native app deferral:

```markdown
## Mobile Access Strategy

Synco provides mobile access through:

1. **Progressive Web App (PWA)** — Install Synco to your home screen from any modern browser. Works on iOS, Android, and desktop. Offline shell with cached assets.
2. **Android Wrapper (TWA)** — A lightweight Android app via Trusted Web Activity for Google Play Store distribution. No native code — Chrome renders the full app.

### Why Not a Native App?

A full native iOS/Android rewrite is **explicitly deferred** until monetization is validated. Rationale:

- PWA + TWA covers 95%+ of mobile use cases (home screen install, fullscreen, offline shell)
- Native development would 3-5x the maintenance burden for a two-person household app
- Users already get phone notifications via Google Calendar sync
- Revenue validation must come before platform expansion

This decision will be revisited after v4.0 proves product-market fit and sustainable revenue.
```
  </action>
  <verify>
    <automated>cd c:\Users\Wojciech\Documents\Python\CalendarPlanner; .venv\Scripts\python -c "
content = open('MONETIZATION.md', encoding='utf-8').read()
assert 'Mobile Access Strategy' in content, 'Missing mobile strategy section'
assert 'deferred' in content.lower(), 'Missing deferral mention'
assert 'PWA' in content, 'Missing PWA mention'
assert 'TWA' in content or 'Trusted Web Activity' in content, 'Missing TWA mention'
print('OK: MONETIZATION.md updated with mobile strategy and native deferral')
"</automated>
  </verify>
  <done>MONETIZATION.md documents mobile access via PWA + TWA and explicitly defers native iOS/Android rewrite. MOB-03 satisfied.</done>
</task>

</tasks>

<verification>
- TWA config has correct app metadata matching manifest.json
- Android README covers both PWABuilder and CLI paths
- Digital Asset Links documentation included
- MONETIZATION.md has mobile strategy section with explicit native deferral
- All existing 354 tests still pass
</verification>

<success_criteria>
- android/twa-config.json is valid JSON with Synco metadata
- android/README.md is comprehensive enough to generate an APK
- MONETIZATION.md explicitly documents native app deferral
- MOB-02 and MOB-03 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/32-mobile-pwa/32-02-SUMMARY.md`
</output>
