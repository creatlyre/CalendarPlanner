---
phase: 10-verify-parser-works-with-polish-language-after-localization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/events/nlp.py
  - app/events/ocr.py
  - app/events/routes.py
autonomous: true
requirements:
  - I18N-07
must_haves:
  truths:
    - "User can parse Polish natural language text in Quick Add and receive structured event fields"
    - "OCR upload path can parse Polish text containing diacritics"
    - "English parsing behavior remains intact after locale-aware changes"
  artifacts:
    - path: "app/events/nlp.py"
      provides: "Locale-aware parser rules for Polish and English"
      exports: ["NLPService.parse"]
    - path: "app/events/ocr.py"
      provides: "OCR reader and parse handoff that supports Polish locale"
      exports: ["OCRService.parse_image"]
    - path: "app/events/routes.py"
      provides: "Locale propagation from request context to parser services"
      exports: ["parse_event", "parse_event_from_image"]
  key_links:
    - from: "app/events/routes.py"
      to: "app/events/nlp.py"
      via: "nlp.parse(..., locale=user_locale)"
      pattern: "parse\(payload\.text, .*locale="
    - from: "app/events/routes.py"
      to: "app/events/ocr.py"
      via: "OCRService().parse_image(..., locale=user_locale)"
      pattern: "parse_image\(image_bytes, .*locale="
---

<objective>
Implement locale-aware parser and OCR contracts so Polish phrases and diacritics are processed correctly without regressing English behavior.

Purpose: Fulfill I18N-07 by hardening parser internals and OCR handoff.
Output: Parser/OCR code paths that accept locale and preserve Polish diacritics through extraction and parsing.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-verify-parser-works-with-polish-language-after-localization/10-CONTEXT.md
@.planning/phases/10-verify-parser-works-with-polish-language-after-localization/10-RESEARCH.md

<interfaces>
From app/events/routes.py:
```python
user_locale = resolve_locale(request)
nlp = NLPService()
result = nlp.parse(payload.text, timezone, context_date)
...
result = OCRService().parse_image(image_bytes, timezone, parsed_context)
```

From app/events/ocr.py:
```python
self._reader = easyocr.Reader(["en"], gpu=False)
parsed = nlp.parse(raw_text, timezone, context_date)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add locale-aware NLP parsing rules for Polish and English</name>
  <files>app/events/nlp.py</files>
  <read_first>
    app/events/nlp.py
    app/events/routes.py
    tests/test_nlp.py
    .planning/phases/10-verify-parser-works-with-polish-language-after-localization/10-CONTEXT.md
  </read_first>
  <behavior>
    - Polish relative/date keywords (for example jutro, dzisiaj, poniedzialek, marzec) produce valid start_at values
    - Title extraction keeps Polish letters (ą ć ę ł ń ó ś ź ż) instead of stripping them
    - Existing English phrases (tomorrow, next Friday, March) still parse as before
  </behavior>
  <action>
    Update NLPService.parse signature to accept locale with default English-safe behavior: `parse(self, text, user_timezone, context_date=None, locale="en")`.
    Add locale normalization helper in nlp.py that maps request locale to parser language set (`pl` or `en`).
    Replace English-only lookup blocks with locale-keyed dictionaries for:
    - relative keywords (`tomorrow/today/yesterday`, `jutro/dzisiaj/wczoraj`)
    - day names (`monday..sunday`, `poniedzialek..niedziela`)
    - month names (`january..december`, `styczen..grudzien`)
    - recurrence keywords (`daily/weekly/monthly/yearly`, `codziennie/co tydzien/co miesiac/co rok`)
    Update title cleanup regex to preserve Unicode letters (do not use ASCII-only `[A-Za-z]`), and keep existing 1-3 word title strategy.
    When using dateparser fallback, pass locale languages list derived from normalized locale.
  </action>
  <acceptance_criteria>
    - app/events/nlp.py contains `def parse(` with `locale=` parameter
    - app/events/nlp.py contains Polish keyword literals `jutro` and `poniedzial`
    - app/events/nlp.py no longer contains ASCII-only pattern `[^A-Za-z\s]`
    - `.venv\\Scripts\\python.exe -m pytest tests/test_nlp.py -q` exits 0 after implementation tasks complete
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_nlp.py -q</automated>
  </verify>
  <done>
    NLP parser supports Polish and English keyword/date parsing while preserving Unicode text in extracted titles.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate locale through OCR route and parser handoff</name>
  <files>app/events/ocr.py, app/events/routes.py</files>
  <read_first>
    app/events/ocr.py
    app/events/routes.py
    app/events/nlp.py
    tests/test_events_api.py
    .planning/phases/10-verify-parser-works-with-polish-language-after-localization/10-CONTEXT.md
  </read_first>
  <behavior>
    - OCR parse route uses request locale for parser invocation
    - OCR service can initialize EasyOCR with Polish support without dropping English support
    - OCR parse handoff to NLP preserves locale and diacritics in raw text
  </behavior>
  <action>
    In app/events/ocr.py:
    - update OCRService constructor and parse_image signature to accept locale (`locale="en"`)
    - initialize reader with a locale-capable list including Polish and English (`["pl", "en"]` or locale-first ordering)
    - pass locale into `NLPService().parse(raw_text, timezone, context_date, locale=locale)`
    In app/events/routes.py:
    - pass `user_locale` into `nlp.parse(..., locale=user_locale)` in `/api/events/parse`
    - pass `user_locale` into `OCRService().parse_image(..., locale=user_locale)` in `/api/events/ocr-parse`
    Keep response schemas unchanged.
  </action>
  <acceptance_criteria>
    - app/events/routes.py contains `nlp.parse(` call with `locale=user_locale`
    - app/events/routes.py contains `parse_image(` call with `locale=user_locale`
    - app/events/ocr.py contains EasyOCR language configuration including `pl`
    - `.venv\\Scripts\\python.exe -m pytest tests/test_events_api.py -k "parse or ocr" -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_events_api.py -k "parse or ocr" -q</automated>
  </verify>
  <done>
    Locale from request flows through parse and OCR endpoints into parser internals, enabling Polish OCR/NLP behavior.
  </done>
</task>

</tasks>

<verification>
- Confirm Polish locale is passed route -> service -> parser in both parse endpoints.
- Confirm OCR service language list includes Polish support and retains English support.
- Confirm focused parser/OCR API tests pass.
</verification>

<success_criteria>
1. Parse endpoints pass locale explicitly into parser services.
2. NLP parser supports Polish keyword/date parsing and diacritic-preserving title extraction.
3. OCR parser handoff is locale-aware and compatible with Polish text extraction.
</success_criteria>

<output>
After completion, create `.planning/phases/10-verify-parser-works-with-polish-language-after-localization/10-01-SUMMARY.md`
</output>
