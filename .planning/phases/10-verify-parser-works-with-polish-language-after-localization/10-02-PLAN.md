---
phase: 10-verify-parser-works-with-polish-language-after-localization
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - tests/test_nlp.py
  - tests/test_events_api.py
autonomous: true
requirements:
  - I18N-07
must_haves:
  truths:
    - "Polish natural language inputs parse into usable event fields with no parser errors on supported patterns"
    - "OCR parse path handles Polish characters and accent variations"
    - "Regression suite confirms no breakage in existing parser and API behavior"
  artifacts:
    - path: "tests/test_nlp.py"
      provides: "Polish NLP parser test corpus"
      contains: "test_parse_polish_"
    - path: "tests/test_events_api.py"
      provides: "Polish OCR route and locale propagation tests"
      contains: "test_ocr_parse_"
  key_links:
    - from: "tests/test_events_api.py"
      to: "app/events/routes.py"
      via: "POST /api/events/parse and /api/events/ocr-parse"
      pattern: "/api/events/parse|/api/events/ocr-parse"
---

<objective>
Validate and lock in Polish parser/OCR behavior with automated tests, including accent-sensitive inputs and regression protection.

Purpose: Prove I18N-07 is met with repeatable test coverage.
Output: Expanded NLP/OCR tests and green regression run.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-verify-parser-works-with-polish-language-after-localization/10-CONTEXT.md
@.planning/phases/10-verify-parser-works-with-polish-language-after-localization/10-RESEARCH.md
@.planning/phases/10-verify-parser-works-with-polish-language-after-localization/10-01-PLAN.md

<interfaces>
From tests/test_nlp.py existing style:
```python
result = nlp_service.parse("dentist tomorrow 2pm", "America/New_York", context_date)
```

From tests/test_events_api.py existing style:
```python
response = authenticated_client.post("/api/events/parse", json={...})
response = authenticated_client.post("/api/events/ocr-parse", data={...}, files={...})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Polish NLP parser tests for keywords, months, and diacritics</name>
  <files>tests/test_nlp.py</files>
  <read_first>
    tests/test_nlp.py
    app/events/nlp.py
    .planning/phases/10-verify-parser-works-with-polish-language-after-localization/10-CONTEXT.md
  </read_first>
  <behavior>
    - Polish relative phrase examples parse successfully (for example: jutro o 14)
    - Polish month/day examples parse with deterministic context_date fixtures
    - Titles containing Polish diacritics remain intact in parsed output
  </behavior>
  <action>
    Add Polish-focused parser tests using the existing fixture style in tests/test_nlp.py.
    Include explicit cases for:
    - relative keywords: `jutro`, `dzisiaj`
    - weekday/month names: `poniedzialek`, `marzec`
    - diacritic-bearing titles: `zażółć gęślą jaźń` or practical event phrases using ą, ć, ę, ł, ń, ó, ś, ź, ż
    Ensure tests pass explicit locale argument (`locale="pl"`) where parser API requires it.
    Add one parity test that English phrase parsing still works with default locale behavior.
  </action>
  <acceptance_criteria>
    - tests/test_nlp.py contains at least three new `test_parse_polish_` cases
    - tests assert on Polish tokens or expected Polish date parsing outcomes
    - `.venv\\Scripts\\python.exe -m pytest tests/test_nlp.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_nlp.py -q</automated>
  </verify>
  <done>
    NLP tests cover Polish phrase parsing and diacritic-preserving title extraction with deterministic assertions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add OCR API tests for Polish locale and diacritic text handoff</name>
  <files>tests/test_events_api.py</files>
  <read_first>
    tests/test_events_api.py
    app/events/routes.py
    app/events/ocr.py
    .planning/phases/10-verify-parser-works-with-polish-language-after-localization/10-CONTEXT.md
  </read_first>
  <behavior>
    - OCR route receives locale and returns parsed Polish title/date fields from mocked OCR output
    - OCR parse behavior remains stable for existing English route tests
  </behavior>
  <action>
    Add API-level tests that monkeypatch `OCRService.parse_image` to emulate Polish OCR text containing diacritics and verify route response shape and values.
    Add a locale propagation assertion by patching parser/OCR call arguments to confirm `locale` equals request locale when using `?lang=pl`.
    Keep existing test style and fixtures (`authenticated_client`, monkeypatch).
  </action>
  <acceptance_criteria>
    - tests/test_events_api.py contains a new OCR localization test case using Polish text with diacritics
    - tests/test_events_api.py asserts locale propagation on parse or OCR route call path
    - `.venv\\Scripts\\python.exe -m pytest tests/test_events_api.py -k "ocr or parse" -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_events_api.py -k "ocr or parse" -q</automated>
  </verify>
  <done>
    API tests prove OCR and parse endpoints support Polish locale behavior and preserve accent-bearing text.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run regression gate for parser/OCR and core localization flows</name>
  <files>tests/test_nlp.py, tests/test_events_api.py, tests/test_calendar_views.py</files>
  <read_first>
    tests/test_nlp.py
    tests/test_events_api.py
    tests/test_calendar_views.py
    .planning/phases/10-verify-parser-works-with-polish-language-after-localization/10-VALIDATION.md
  </read_first>
  <action>
    Run the focused regression command:
    `.venv\\Scripts\\python.exe -m pytest tests/test_nlp.py tests/test_events_api.py tests/test_calendar_views.py -q`
    If failures indicate locale regression, fix parser/OCR locale handling in code from 10-01 and rerun until green.
  </action>
  <acceptance_criteria>
    - focused regression command exits 0
    - no failing tests in NLP, events API, or calendar localization suite
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_nlp.py tests/test_events_api.py tests/test_calendar_views.py -q</automated>
  </verify>
  <done>
    Regression gate confirms Polish parser/OCR support with no breakage in core localized user flows.
  </done>
</task>

</tasks>

<verification>
- Validate Polish parser/OCR tests are present and green.
- Validate locale propagation assertions exist in API tests.
- Validate focused regression command is green.
</verification>

<success_criteria>
1. Test coverage proves Polish NLP and OCR parsing behavior (including diacritics).
2. Parse/OCR locale propagation is verified in API tests.
3. Regression suite remains green for parser and localization-critical views.
</success_criteria>

<output>
After completion, create `.planning/phases/10-verify-parser-works-with-polish-language-after-localization/10-02-SUMMARY.md`
</output>
