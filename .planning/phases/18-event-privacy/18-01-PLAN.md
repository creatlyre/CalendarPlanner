---
phase: 18-event-privacy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/partials/month_grid.html
  - app/templates/partials/day_events.html
  - app/templates/partials/event_form.html
autonomous: true
requirements: [VIS-01, VIS-03]

must_haves:
  truths:
    - "Private events show a 🔒 icon before their title in the month grid cells"
    - "Private events show a 🔒 icon before their title in the day view list"
    - "The simple event form (event_form.html) contains a visibility dropdown with shared/private options"
  artifacts:
    - path: "app/templates/partials/month_grid.html"
      provides: "Lock icon rendering for private events in month calendar"
      contains: "event.visibility == 'private'"
    - path: "app/templates/partials/day_events.html"
      provides: "Lock icon rendering for private events in day list"
      contains: "event.visibility == 'private'"
    - path: "app/templates/partials/event_form.html"
      provides: "Visibility select dropdown in simple event form"
      contains: "event-visibility"
  key_links:
    - from: "app/templates/partials/month_grid.html"
      to: "event.visibility"
      via: "Jinja2 if-check on event.visibility"
      pattern: "event\\.visibility.*private.*🔒"
    - from: "app/templates/partials/day_events.html"
      to: "event.visibility"
      via: "Jinja2 if-check on event.visibility"
      pattern: "event\\.visibility.*private.*🔒"
---

<objective>
Add visual privacy indicators and form completeness for event visibility.

Purpose: Users who own private events need a clear lock icon on both the month grid and day view so they know which events are hidden from their partner. The simple event form also needs the visibility dropdown for form parity.
Output: Updated month_grid.html, day_events.html, event_form.html templates
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-event-privacy/18-CONTEXT.md

<interfaces>
<!-- Event model visibility field — already exists in database/models.py -->
<!-- event.visibility is either "shared" or "private" (string) -->
<!-- event.created_by_user_id is the owner's UUID -->
<!-- Private events are ALREADY filtered by _visible_to() in repository — so any private event in the template's event list belongs to the current user. No need to check ownership in templates. -->

From app/events/schemas.py:
```python
visibility: Literal["shared", "private"] = "shared"
```

Existing visibility dropdown pattern (from event_entry_modal.html):
```html
<label for="event-entry-visibility" class="mb-1 block text-xs font-medium uppercase tracking-wide text-white/60">{{ t('qa.visibility') }}</label>
<select id="event-entry-visibility" class="glass-input">
  <option value="shared">{{ t('qa.visibility_shared') }}</option>
  <option value="private">{{ t('qa.visibility_private') }}</option>
</select>
<p class="mt-1 text-xs text-white/40">{{ t('qa.visibility_help') }}</p>
```

i18n keys already exist in en.json and pl.json:
- qa.visibility, qa.visibility_shared, qa.visibility_private, qa.visibility_help
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lock icons to month grid and day view templates</name>
  <files>app/templates/partials/month_grid.html, app/templates/partials/day_events.html</files>
  <read_first>
    - app/templates/partials/month_grid.html (current event rendering in grid cells)
    - app/templates/partials/day_events.html (current event rendering in day list)
    - app/templates/partials/event_entry_modal.html (reference for visibility dropdown pattern)
  </read_first>
  <action>
**month_grid.html** — Add 🔒 emoji before event title for private events.

Find the event rendering line inside the `{% for event in items[:2] %}` loop:
```html
<div class="truncate mt-0.5 text-xs bg-indigo-500/25 text-indigo-200 rounded px-1 py-0.5" title="{{ event.start_at.strftime('%H:%M') }} {{ event.title }}">{{ event.start_at.strftime('%H:%M') }} {{ event.title }}</div>
```

Replace with:
```html
<div class="truncate mt-0.5 text-xs bg-indigo-500/25 text-indigo-200 rounded px-1 py-0.5" title="{{ event.start_at.strftime('%H:%M') }} {{ event.title }}">{{ event.start_at.strftime('%H:%M') }} {% if event.visibility == 'private' %}🔒 {% endif %}{{ event.title }}</div>
```

**day_events.html** — Add 🔒 emoji before event title for private events.

Find the event title line:
```html
<div class="font-medium text-white">{{ event.title }}</div>
```

Replace with:
```html
<div class="font-medium text-white">{% if event.visibility == 'private' %}🔒 {% endif %}{{ event.title }}</div>
```

No background color change per user decision — lock icon is the sole indicator. No lock in the "+N more" overflow count per user decision.
  </action>
  <verify>
    <automated>python -c "import pathlib; mg=pathlib.Path('app/templates/partials/month_grid.html').read_text(); de=pathlib.Path('app/templates/partials/day_events.html').read_text(); assert \"event.visibility == 'private'\" in mg, 'month_grid missing visibility check'; assert '🔒' in mg, 'month_grid missing lock icon'; assert \"event.visibility == 'private'\" in de, 'day_events missing visibility check'; assert '🔒' in de, 'day_events missing lock icon'; print('PASS: Lock icons present in both templates')"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `event.visibility == 'private'` returns matches in month_grid.html (at least 1 match)
    - grep for `🔒` returns matches in month_grid.html (at least 1 match)
    - grep for `event.visibility == 'private'` returns matches in day_events.html (at least 1 match)
    - grep for `🔒` returns matches in day_events.html (at least 1 match)
    - The lock icon appears BETWEEN the time and the title in month_grid.html (inside the `{% for event in items[:2] %}` block)
    - The lock icon appears BEFORE `{{ event.title }}` in day_events.html (inside the `{% for event in events %}` block)
    - The "+N more" overflow div is NOT modified (no lock in overflow count)
  </acceptance_criteria>
  <done>Lock icon 🔒 renders before private event titles in both month grid and day view</done>
</task>

<task type="auto">
  <name>Task 2: Add visibility dropdown to simple event form</name>
  <files>app/templates/partials/event_form.html</files>
  <read_first>
    - app/templates/partials/event_form.html (current form fields)
    - app/templates/partials/event_entry_modal.html (reference: existing visibility dropdown pattern, lines 38-44)
  </read_first>
  <action>
**event_form.html** — Add a visibility dropdown between the repeat_until field and the submit buttons.

After the existing `<div>` containing `id="event-repeat-until"` and before the `<div class="flex flex-wrap gap-2">` button row, insert:

```html
<div>
    <label class="mb-1 block text-xs font-medium uppercase tracking-wide text-white/60">{{ t('qa.visibility') }}</label>
    <select id="event-visibility" class="glass-input">
        <option value="shared">{{ t('qa.visibility_shared') }}</option>
        <option value="private">{{ t('qa.visibility_private') }}</option>
    </select>
    <p class="mt-1 text-xs text-white/40">{{ t('qa.visibility_help') }}</p>
</div>
```

This uses the same i18n keys as the entry modal: `qa.visibility`, `qa.visibility_shared`, `qa.visibility_private`, `qa.visibility_help`. The select id is `event-visibility` (not `event-entry-visibility`, to avoid collision with the modal form).

Note: event_form.html is not currently included in calendar.html (the entry modal is the primary form). This change ensures form parity per user decision so both creation paths support privacy when event_form.html is used.
  </action>
  <verify>
    <automated>python -c "import pathlib; ef=pathlib.Path('app/templates/partials/event_form.html').read_text(); assert 'event-visibility' in ef, 'missing visibility select id'; assert 'qa.visibility' in ef, 'missing i18n key'; assert 'shared' in ef and 'private' in ef, 'missing option values'; print('PASS: Visibility dropdown in event_form.html')"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `id="event-visibility"` returns a match in event_form.html
    - grep for `qa.visibility` returns matches in event_form.html (label and help text)
    - grep for `value="shared"` returns a match in event_form.html
    - grep for `value="private"` returns a match in event_form.html
    - The visibility dropdown appears after the repeat_until input and before the button row
  </acceptance_criteria>
  <done>Simple event form has visibility dropdown with shared/private options using existing i18n keys</done>
</task>

</tasks>

<verification>
python -m pytest tests/ -x -q 2>&1 | Select-Object -Last 10
All existing tests pass (no backend changes in this plan, only template modifications).
</verification>

<success_criteria>
- 🔒 emoji appears before private event titles in month_grid.html
- 🔒 emoji appears before private event titles in day_events.html
- event_form.html contains visibility dropdown with shared/private options
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/18-event-privacy/18-01-SUMMARY.md`
</output>
